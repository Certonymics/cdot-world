---
import Layout from "../layouts/Layout.astro";
import AppGallery from "../components/AppGallery.astro";
import StoreDropdown from "../components/StoreDropdown.astro";

/* These follow the TAGLINE, not the hero. The hero names the four products -
   "identity, email, messaging, and payments" - where the tagline collapses the
   middle two into "communication". It is the tagline that is baked into og.png
   and sits in the header of every page, so a search result or a shared link
   that reads differently from the card beside it would look like a different
   product. These track the line that travels. */
const title = "cDot: Secure identity, communication and payments";
const description =
  "When apps and websites track everything you do, stay private online with cDot: the app for secure identity, communication and payments.";
const ogTitle = "cDot — Secure identity, communication and payments";
const ogDescription =
  "When apps and websites track everything you do, stay private online with cDot. Share only what you want, when you want — across identity, communication and payments.";

/* Ported from HowItWorks.jsx in 'Cdot site (main)'. Its `highlight` and
   `features` branches are dropped - no step there sets either. */
const HOW = [
  ["Download the app", "Get the free cDot app for iOS or Android."],
  ["Connect your email", "You’ll be able to send encrypted emails with your existing address, plus prove your identity and pay with just your email."],
  ["Scan your ID and take a selfie", "A one-time check that encrypts your identity information into anonymous attribute proofs."],
  ["Start using cDot", "Your encrypted identity is now tied to your email, powering private messaging, payments, and verification."],
];
---

<Layout title={title} description={description} ogTitle={ogTitle} ogDescription={ogDescription}>

<div class="hero">
  <div class="watermark" aria-hidden="true">
    <img src="/assets/Calque_1.svg" alt="" width="283" height="338" decoding="async">
  </div>
  <div class="wrap">
    <h1>The app for <span class="glow">secure identity, email, messaging, and payments</span>.</h1>
    <p class="sub">When apps and websites track everything you do, stay private online with cDot. Share only what you want, when you want.</p>
    <div class="hero-ctas">
      <StoreDropdown label="Download the app" id="dl-home" />
      <a class="btn btn-ghost" href="/network">Discover the network</a>
    </div>
  </div>
</div>

<AppGallery />

<div class="divider"></div>

<section id="how-it-works">
  <div class="wrap">
    <h2>How it works</h2>
    <p class="lead">Protect your privacy in just a few minutes.</p>
    <!-- An <ol>, so the order is in the markup rather than only in the styling:
         the numbers are drawn by a CSS counter, and a reader who never sees
         them still gets a numbered list. -->
    <ol class="hiw">
      {HOW.map(([t, b]) => (
        <li>
          <div class="hiw-card">
            <h3>{t}</h3>
            <p>{b}</p>
          </div>
        </li>
      ))}
    </ol>
  </div>
</section>

<div class="divider"></div>

<!-- Ported from WhyCdotExists.jsx in 'Cdot site (main)'. A teaser: the full
     version belongs on /about, which the closing link points at. -->
<section id="why">
  <div class="wrap">
    <h2>Why cDot exists</h2>
    <p class="lead">The internet never came with a way to prove who you are, keep your data private, or move money safely. cDot is the internet&rsquo;s missing piece: letting you move across the internet untracked and ensuring your money stays yours, whatever happens. Our network is run by a worldwide community — and because it&rsquo;s open-source, anyone can build on it.</p>
    <p class="lead why-more"><a href="/about">Learn more about cDot</a></p>
  </div>
</section>

<div class="divider"></div>

<!-- Ported from NoDataStatement.jsx in 'Cdot site (main)'. Left-aligned and on
     our tokens; needs no styles of its own, being a heading and two leads. -->
<section id="privacy">
  <div class="wrap">
    <h2>Our goal is to protect your data, not collect it.</h2>
    <p class="lead">Most apps track your activity and harvest information about you. With cDot, you share anonymous proofs, not personal data, so you only reveal what you choose to. With no data business to protect, cDot is community-run, open-source, and borderless. You&rsquo;re in control of your identity, funds, and private keys. Our system just makes sure you never lose them.</p>
  </div>
</section>

<div class="divider"></div>

<section id="contact">
  <div class="wrap">
    <h2>Want to know more? Get in touch.</h2>
    <form id="contactForm">
    <div class="contact-grid">
        <div class="contact-fields">
          <div class="field">
            <label for="cf-email">Email</label>
            <input id="cf-email" type="email" name="email" autocomplete="email" required>
          </div>
          <div class="field">
            <label for="cf-msg">Message</label>
            <textarea id="cf-msg" name="message" maxlength="5000" required></textarea>
          </div>
          <!-- Honeypot: hidden from people, filled in by bots. -->
          <input class="cf-honeypot" type="text" name="website" tabindex="-1" autocomplete="off" aria-hidden="true">
          <!-- Replaces the fields above on success; also carries error text. -->
          <p class="cf-status" id="cf-status" role="status" aria-live="polite" tabindex="-1"></p>
        </div>
      <noscript>
        <p class="cf-status is-error">This form needs JavaScript. Please email
        <a href="mailto:info@certonymity.com">info@certonymity.com</a> instead.</p>
      </noscript>
      <div class="addr">
        <p>cDot — built by Certonymity</p>
        <p><a href="mailto:info@certonymity.com">info@certonymity.com</a></p>
        <p>Certonymity Ltd<br>86-87 Campden Street<br>London<br>W8 7EN</p>
      </div>
    </div>
    <button class="btn btn-primary" type="submit" style="margin-top:18px">Send message</button>
    </form>
  </div>
</section>

<!-- Contact form. POSTs to the Google Apps Script in scripts/contact-form.gs,
     which records the enquiry in a sheet and emails the team. That script is a
     separate deployment from the c.Email one - see its header comment. -->
<script is:inline>
(function(){
  /* Public by design - see the header comment in scripts/contact-form.gs.
     Replace with the /exec URL of the deployed cDot contact-form script. */
  const CONTACT_FORM_URL =
    'https://script.google.com/macros/s/AKfycbzkT4I_iMLiBBKRZeKdSEU2ohJVpfOFd4UA-tk2ss4p2YddFDbvU1OFPnjHcr0RaOHdUA/exec';

  const form = document.getElementById('contactForm');
  const statusEl = document.getElementById('cf-status');
  const button = form.querySelector('button[type="submit"]');
  const fields = form.querySelectorAll('.contact-fields .field, .cf-honeypot');
  let sending = false;

  /* The status line stays in the DOM and is emptied rather than hidden: a
     display:none live region is not monitored, so un-hiding and filling it in
     one task announces unreliably. */
  function show(message, isError){
    statusEl.textContent = message;
    statusEl.classList.toggle('is-error', !!isError);
    /* Errors move focus, so a screen-reader user is not left waiting for an
       announcement that may not arrive. */
    if(isError) statusEl.focus();
  }

  /* On success the fields and the button go away entirely, so there is no live
     Send button sitting under a thank-you message inviting a second submission. */
  function succeed(message){
    fields.forEach(function(el){ el.hidden = true; });
    button.hidden = true;
    show(message, false);
  }

  function fail(message){
    show(message || 'Something went wrong. Please try again, or email us directly at info@certonymity.com.', true);
    sending = false;
    button.disabled = false;
    button.textContent = 'Send message';
  }

  form.addEventListener('submit', async function(e){
    e.preventDefault();
    if (sending) return;

    /* Better to say so than to POST into the void and report success. */
    if (!CONTACT_FORM_URL) {
      return fail('This form is not connected yet. Please email us directly at info@certonymity.com.');
    }

    /* Sending state lives on the button only. Putting it in the status line too
       would shift the layout mid-submit, since the status sits in the field column. */
    sending = true;
    button.disabled = true;
    button.textContent = 'Sending…';
    statusEl.textContent = '';

    try {
      /* No Content-Type header on purpose. Setting one makes this a preflighted
         request, and Apps Script does not answer the OPTIONS probe, so the whole
         submission fails before it starts. Sent as text/plain it is a "simple
         request" and goes straight through; doPost reads the raw body regardless. */
      /* Without a timeout a stalled connection leaves the button disabled on
         "Sending…" forever, with no way to retry. */
      const response = await fetch(CONTACT_FORM_URL, {
        signal: AbortSignal.timeout(15000),
        method: 'POST',
        body: JSON.stringify({
          email: document.getElementById('cf-email').value.trim(),
          message: document.getElementById('cf-msg').value.trim(),
          website: form.querySelector('[name="website"]').value
        })
      });
      const result = await response.json().catch(function(){ return null; });
      /* Only confirm what the server actually confirmed. */
      if (!response.ok || !result || !result.ok) return fail(result && result.error);

      succeed('Thanks — your message is on its way. We’ll reply to the address you gave us.');
    } catch (err) {
      fail();
    }
  });
})();
</script>

</Layout>
